"""Test package marker.

`unittest discover` requires the start directory to be importable; without this
file the suites can only be run by naming each module explicitly, which is how
new test files ended up outside CI.
"""
