# Copy to .env and fill in real values. NEVER commit .env.

# Leave as /api for local development — Vite proxies /api to the backend
# (see vite.config.js). Set to the full backend URL for deployed builds.
VITE_API_URL=/api
